import Layout from "@/component/layout/Layout"
import UserSettings from "@/component/settings/Settings"

const Settings = () => {
    return (
        <Layout>
            <UserSettings />
        </Layout>
    )
}
export default Settings