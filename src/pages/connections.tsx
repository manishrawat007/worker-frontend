import UserList from "@/component/connections/Follower"
import Layout from "@/component/layout/Layout"

const Connections = () => {
    return (
        <Layout>
            <UserList/>
        </Layout>
    )
}
export default Connections